barbearia-mvp/│├── index.html                  # Landing page ou redirecionamento inicial├── login.html                  # Tela de autenticação (Login / Cadastro)├── dashboard.html              # Tela principal (Painel do Salão / Agendamentos)├── clientes.html               # Tela de gestão de clientes do salão│├── css/                        # Arquivos CSS separados da lógica (Conforme padrão)│   ├── global.html / .css      # Variáveis, reset e estilos base (cores, fontes)│   ├── auth.css                # Estilos específicos para as telas de login/cadastro│   └── dashboard.css           # Estilos para o painel, tabelas e grids│├── js/│   ├── config/│   │   └── supabase.js         # Inicialização do cliente Supabase e chaves│   ├── services/│   │   ├── auth.service.js     # Lógica de login, cadastro, logout e checkAuth│   │   └── tenant.service.js   # Lógica de chamadas ao banco (agendamentos, clientes)│   └── pages/│       ├── login.js            # Manipulação de eventos da tela de login│       └── dashboard.js        # Manipulação de eventos e renderização do painel│└── assets/                     # Imagens, ícones e fontes locais (se houver)